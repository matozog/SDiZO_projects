This projects implements and tests graphs algorithms depending on the size of instance and how graph is represented in computer's memory.


Example file look like:
in first line we have -> amountOfEdges amountOfTops top_start top_end
each other line have -> top_start top_end weight


example file is in directory